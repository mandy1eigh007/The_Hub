# Provenance

## Source Material

- The workspace was restored from a single uploaded Replit export zip.
- That export was the only available history source.
- There is no separate workspace memory or additional Replit backup to recover from.

## Practical Meaning

- Treat the files in this repository as the best available current snapshot.
- When documenting future work, write down assumptions and decisions in `handoff/` as they happen.
- If a file looks important, preserve it unless the user explicitly asks to remove it.

## What To Record Going Forward

- Behavior changes and why they were made.
- Runtime or deployment requirements.
- Any schema, persistence, or auth changes.
- Any manually inspected export artifacts that future work depends on.